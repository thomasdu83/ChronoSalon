# Chat room backgrounds

Expected generated assets:

- historical-scene.png
- cross-time.png

These files are loaded by frontend/scripts/app.js according to room_type. If they are missing, the UI falls back to CSS-only themed backgrounds.
